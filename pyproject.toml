[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-forge-listener"
version = "0.1.0"
description = "Continuous fleet listener — event classification, framing, P0 compliance"
license = {text = "MIT"}
requires-python = ">=3.8"
